apiVersion: v1
kind: Service
metadata:
  name: kafka-svc
  labels:
    app: kafka
    name: kafka
spec:
  selector:
    app: kafka
    name: kafka
  ports:
    - protocol: TCP
      port: 9092
      targetPort: 9092
      name: kafka-broker